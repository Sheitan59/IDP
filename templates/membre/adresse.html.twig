{% extends 'base.html.twig' %}

{% block title %}Mes Adresse - Indoor Proxy Garden{% endblock %}

{% block content %}
    <a href="{{ path('membre_adresse_add') }}" class="btn btn-info float-end">Ajouter une adresse</a>
    <h1>Mes adresses</h1>
    C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.<br/>
    <a href="{{ path('membre') }}">Retour</a>
    <hr>
    {% if app.user.adresses|length == 0 %}
        <p class="text-center">
            Vous n'avez pas encore ajouté d'adresse dans votre compte client. Pour en ajouter une, veuillez <a href="{{ path('membre_adresse_add') }}">cliquer ici</a>.
        </p>
    {% else %}
        <div class="row">
            {% for adresse in app.user.adresses %}
                <div class="col-4">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">{{ adresse.surnom }}</h5>
                            <p class="card-text">
                                {{ adresse.adresse }},<br/>
                                {{ adresse.codePostal }} - {{ adresse.ville }} - {{ adresse.pays }}
                            </p>
                            <a href="{{ path('membre_adresse_edit', { 'id' : adresse.id }) }}">Modifier</a> | <a href="{{ path('membre_adresse_delete', { 'id' : adresse.id }) }}">Supprimer</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
{% endblock %}