{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Indoor Proxy Garden{% endblock %}

{% block content %}
<style>
    .changPwd { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .changPwd code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="changPwd">
    <h1>Bienvenu {{ app.user.prenom }} </h1>
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}
</div>
{% endblock %}
